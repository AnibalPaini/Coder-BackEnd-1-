<div>
    <h1>Home</h1>
    <h2>Productos</h2>
    <ul>
        {{#each products}}
            <li>
                <p>{{this.title}}</p>
                <p>{{this.description}}</p>
                <p>${{this.price}}</p>
            </li>
        {{/each}}
    </ul>
</div>